# 🚀 Полный Деплой指南

## 📋 Обзор

Полное руководство по деплою фронтенда и бэкенда для проекта Skin Factory.

---

## 🎯 Деплой Бэкенда на Render

### Шаг 1: Подготовка репозитория

```bash
# Перейдите в папку бэкенда
cd backend_tg

# Инициализируйте git (если еще нет)
git init
git add .
git commit -m "Production ready backend"

# Создайте репозиторий на GitHub и подключите его
git remote add origin https://github.com/YOUR_USERNAME/ArFintes2.git
git push -u origin main
```

### Шаг 2: Создание базы данных на Render

1. Перейдите на https://dashboard.render.com/
2. Нажмите "New+" → "PostgreSQL"
3. Заполните:
   - Name: `skin-factory-db`
   - Region: `Frankfurt` (ближе к пользователям)
   - Plan: `Free`
4. Нажмите "Create Database"
5. Скопируйте строку подключения (Database URL)

### Шаг 3: Создание Web Service

#### Через GitHub (рекомендуется)

1. В Render нажмите "New+" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Render автоматически использует `render.yaml`
4. Нажмите "Create Web Service"

#### Вручную

1. В Render нажмите "New+" → "Web Service"
2. Подключите GitHub репозиторий
3. Настройте:
   - Name: `ar-fintes2`
   - Region: `Frankfurt`
   - Branch: `main`
   - Runtime: `Node`
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
4. Добавьте переменные окружения (см. ниже)
5. Нажмите "Create Web Service"

### Шаг 4: Переменные окружения

Добавьте в настройки Web Service следующие переменные:

```bash
NODE_ENV=production
PORT=10000
DATABASE_URL=postgresql://... (из шага 2)
JWT_SECRET=сгенерировать_случайный_ключ_минимум_32_символа
BACKEND_URL=https://ar-fintes2.onrender.com
FRONTEND_URL=https://tg-frontend-7ltg.vercel.app
ALLOWED_ORIGINS=https://tg-frontend-7ltg.vercel.app,https://*.vercel.app
TELEGRAM_BOT_TOKEN=8550648832:AAFey51LR3SiWIf0r91iSBwfYg3vWNEl_AQ
TELEGRAM_BOT_USERNAME=SkinFactoryArBot
TELEGRAM_WEB_APP_URL=https://tg-frontend-7ltg.vercel.app
ADMIN_IDS=777777777,123456789
```

### Шаг 5: Настройка Webhook в Telegram

1. Откройте бота @BotFather в Telegram
2. Отправьте команду `/setwebhook`
3. Выберите своего бота `@SkinFactoryArBot`
4. Введите URL:
   ```
   https://ar-fintes2.onrender.com/api/bot/webhook
   ```
5. Получите подтверждение: `Webhook was set`

### Шаг 6: Проверка работы

```bash
# Здоровье сервера
curl https://ar-fintes2.onrender.com/api/health

# Подключение к базе данных
curl https://ar-fintes2.onrender.com/api/health/db

# Webhook
curl https://ar-fintes2.onrender.com/api/bot/webhook
```

---

## 🎨 Деплой Фронтенда на Vercel

### Шаг 1: Подготовка

```bash
# Перейдите в папку фронтенда
cd tg_frontend

# Проверьте .env.production
cat .env.production
```

Убедитесь, что `.env.production` содержит:

```bash
VITE_API_URL=https://ar-fintes2.onrender.com/api
VITE_TELEGRAM_BOT_USERNAME=SkinFactoryArBot
VITE_TELEGRAM_WEB_APP_URL=https://t.me/SkinFactoryArBot/skin_factory
```

### Шаг 2: Деплой через Vercel CLI

```bash
# Установите Vercel CLI (если еще нет)
npm install -g vercel

# Войдите в Vercel
vercel login

# Задеплойте
vercel

# Следуйте инструкциям:
# - Set up and deploy? Yes
# - Which scope? Ваш аккаунт
# - Link to existing project? No
# - Project name: tg-frontend
# - In which directory is your code located? ./tg_frontend
# - Override settings? No

# Для production
vercel --prod
```

### Шаг 3: Деплой через Vercel Dashboard

1. Перейдите на https://vercel.com/new
2. Импортируйте репозиторий GitHub
3. Настройте:
   - Project Name: `tg-frontend`
   - Framework Preset: `Vite`
   - Root Directory: `tg_frontend`
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Добавьте переменные окружения:
   ```bash
   VITE_API_URL=https://ar-fintes2.onrender.com/api
   VITE_TELEGRAM_BOT_USERNAME=SkinFactoryArBot
   VITE_TELEGRAM_WEB_APP_URL=https://t.me/SkinFactoryArBot/skin_factory
   ```
5. Нажмите "Deploy"

### Шаг 4: Настройка Web App в Telegram

1. Откройте @BotFather
2. Отправьте `/newapp`
3. Выберите бота `@SkinFactoryArBot`
4. Введите название: `Skin Factory`
5. Введите описание: `Открывай кейсы, получай скины!`
6. Введите URL:
   ```
   https://tg-frontend-7ltg.vercel.app
   ```
7. Получите короткий URL: `https://t.me/SkinFactoryArBot/skin_factory`

---

## 🧪 Тестирование после деплоя

### Тестирование бэкенда

```bash
# Проверка здоровья
curl https://ar-fintes2.onrender.com/api/health

# Проверка авторизации
curl -X POST https://ar-fintes2.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "telegramId": "123456789",
    "username": "test_user",
    "firstName": "Test",
    "lastName": "User"
  }'
```

### Тестирование фронтенда

1. Откройте: https://tg-frontend-7ltg.vercel.app
2. Проверьте загрузку страниц
3. Проверьте авторизацию через Telegram

### Тестирование бота

1. Откройте бота: @SkinFactoryArBot
2. Отправьте `/start`
3. Нажмите "🎮 Открыть приложение"
4. Проверьте авторизацию
5. Проверьте команды `/play`, `/check_subs`, `/stats`

---

## 🔍 Мониторинг и отладка

### Логи на Render

1. Перейдите в Dashboard Render
2. Выберите ваш Web Service
3. Нажмите "Logs"
4. Просматривайте логи в реальном времени

### Логи на Vercel

1. Перейдите в Dashboard Vercel
2. Выберите проект
3. Нажмите "Deployments"
4. Выберите нужный деплой
5. Нажмите "View Function Logs"

### Проверка вебхука

```bash
# Проверить статус вебхука
curl https://api.telegram.org/bot8550648832:AAFey51LR3SiWIf0r91iSBwfYg3vWNEl_AQ/getWebhookInfo

# Удалить вебхук (для отладки в polling режиме)
curl https://api.telegram.org/bot8550648832:AAFey51LR3SiWIf0r91iSBwfYg3vWNEl_AQ/deleteWebhook
```

---

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверьте вебхук:
   ```bash
   curl https://api.telegram.org/botTOKEN/getWebhookInfo
   ```
2. Проверьте логи на Render
3. Убедитесь, что сервер отвечает на `/api/bot/webhook`

### CORS ошибки

1. Проверьте `ALLOWED_ORIGINS` в переменных окружения
2. Проверьте конфигурацию CORS в бэкенде
3. Убедитесь, что URL фронтенда добавлен в список

### База данных недоступна

1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что база данных запущена на Render
3. Проверьте логи на предмет ошибок подключения

### Фронтенд не подключается к API

1. Проверьте `VITE_API_URL` в `.env.production`
2. Убедитесь, что бэкенд запущен
3. Проверьте консоль браузера на ошибки
4. Проверьте Network вкладку в DevTools

---

## 📊 Оптимизация производительности

### Бэкенд

- ✅ Используйте connection pooling для PostgreSQL
- ✅ Включите кеширование для часто запрашиваемых данных
- ✅ Настройте CDN для статических файлов
- ✅ Оптимизируйте запросы к БД

### Фронтенд

- ✅ Vite автоматически оптимизирует бандл
- ✅ Используйте ленивую загрузку для больших компонентов
- ✅ Оптимизируйте изображения
- ✅ Включите кеширование в браузере

---

## 🔐 Безопасность

### Бэкенд

- ✅ Используйте HTTPS (автоматически на Render)
- ✅ Валидируйте все входные данные
- ✅ Используйте параметризованные запросы к БД
- ✅ Настройте rate limiting
- ✅ Регулярно обновляйте зависимости

### Фронтенд

- ✅ Не храните секреты в клиентском коде
- ✅ Валидируйте данные на клиенте
- ✅ Используйте CSP заголовки
- ✅ Обновляйте зависимости

---

## 📞 Поддержка

- Telegram: @SkinFactoryArBot
- Email: support@skinfactory.com
- GitHub: https://github.com/bhgou/tg_frontend

---

**Версия:** 2.0.0  
**Дата обновления:** 2026-02-13  
**Статус:** ✅ Production Ready
