# Production Deployment Guide

## 📋 Обзор

Сервер полностью готов к production деплою на Render.com. Все компоненты протестированы и работают корректно.

## ✅ Что настроено

### 1. База данных PostgreSQL
- ✅ Подключение к базе данных на Render
- ✅ Все таблицы созданы автоматически
- ✅ Пул соединений настроен

### 2. Express API сервер
- ✅ CORS настроен для фронтенда на Vercel
- ✅ Безопасность: Helmet, CORS, rate limiting
- ✅ Логирование с Morgan
- ✅ Health checks: `/api/health`, `/api/health/db`

### 3. Telegram бот
- ✅ Webhook endpoint: `/api/bot/webhook`
- ✅ Команды: `/start`, `/play`, `/check_subs`, `/stats`
- ✅ Web App интеграция
- ✅ Автоматический запуск в production режиме

### 4. API эндпоинты
- ✅ Auth: `/api/auth/*`
- ✅ Users: `/api/users/*`
- ✅ Cases: `/api/cases/*`
- ✅ Games: `/api/games/*`
- ✅ Market: `/api/market/*`
- ✅ Payments: `/api/payments/*`
- ✅ Bot: `/api/bot/*`

## 🚀 Деплой на Render

### Шаг 1: Создание базы данных

1. Перейдите на https://dashboard.render.com/
2. Создайте новый PostgreSQL Database
3. Скопируйте строку подключения (Database URL)

### Шаг 2: Настройка переменных окружения

В настройках сервиса на Render добавьте следующие переменные:

```bash
NODE_ENV=production
PORT=10000
DATABASE_URL=postgresql://... (из шага 1)
JWT_SECRET=ваш_секретный_ключ
BACKEND_URL=https://ar-fintes2.onrender.com
FRONTEND_URL=https://tg-frontend-7ltg.vercel.app
ALLOWED_ORIGINS=https://tg-frontend-7ltg.vercel.app,https://*.vercel.app
TELEGRAM_BOT_TOKEN=8550648832:AAFey51LR3SiWIf0r91iSBwfYg3vWNEl_AQ
TELEGRAM_BOT_USERNAME=SkinFactoryArBot
TELEGRAM_WEB_APP_URL=https://tg-frontend-7ltg.vercel.app
ADMIN_IDS=777777777,123456789
```

### Шаг 3: Деплой

#### Через GitHub (рекомендуется)
1. Запушьте код на GitHub
2. В Render создайте новый Web Service
3. Подключите репозиторий
4. Render автоматически использует `render.yaml` для конфигурации

#### Вручную
1. Создайте Web Service в Render
2. Укажите:
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
3. Добавьте все переменные окружения

### Шаг 4: Настройка Webhook

После успешного деплоя:

1. Перейдите в Telegram BotFather
2. Отправьте команду `/setwebhook`
3. Выберите своего бота
4. Введите URL: `https://ar-fintes2.onrender.com/api/bot/webhook`

### Шаг 5: Проверка работы

Проверьте следующие эндпоинты:

```bash
# Здоровье сервера
curl https://ar-fintes2.onrender.com/api/health

# Подключение к базе данных
curl https://ar-fintes2.onrender.com/api/health/db

# Webhook
curl https://ar-fintes2.onrender.com/api/bot/webhook
```

## 🧪 Тестирование бота

После деплоя протестируйте бота в Telegram:

1. Откройте бота: `@SkinFactoryArBot`
2. Отправьте `/start` - должно появиться меню с кнопкой "🎮 Открыть приложение"
3. Нажмите кнопку - должно открыться приложение на Vercel
4. Проверьте команду `/play`
5. Проверьте команду `/check_subs`

## 📊 Мониторинг

### Логи
- Логи доступны в панели Render: Logs
- Все ошибки логируются в консоль

### Health Checks
- `/api/health` - общий статус сервера
- `/api/health/db` - подключение к базе данных

### Метрики
- Uptime сервера
- Подключения к базе данных
- Количество пользователей
- Статистика бота

## 🔒 Безопасность

### Настроено:
- ✅ CORS для разрешенных доменов
- ✅ Helmet для HTTP заголовков
- ✅ Rate limiting (100 запросов за 15 минут)
- ✅ Валидация JWT токенов
- ✅ Защита от SQL инъекций (parameterized queries)
- ✅ Пароли хешируются с bcrypt

### Рекомендуется:
- 🔐 Изменить `JWT_SECRET` на случайное значение
- 🔐 Использовать HTTPS (автоматически на Render)
- 🔐 Настроить мониторинг (Sentry, etc.)
- 🔐 Регулярно обновлять зависимости

## 🐛 Troubleshooting

### Бот не отвечает
1. Проверьте логи в Render
2. Убедитесь, что webhook настроен правильно: `/setwebhook`
3. Проверьте переменную `TELEGRAM_BOT_TOKEN`
4. Проверьте, что сервер отвечает на `/api/bot/webhook`

### Ошибка подключения к базе данных
1. Проверьте `DATABASE_URL`
2. Убедитесь, что база данных запущена
3. Проверьте логи для деталей ошибки

### CORS ошибки
1. Проверьте `ALLOWED_ORIGINS`
2. Убедитесь, что URL фронтенда добавлен
3. Проверьте заголовки ответа

### Сервер падает
1. Проверьте логи в Render
2. Убедитесь, что все переменные окружения заданы
3. Проверьте использование памяти (Render free tier: 512MB)

## 📝 Дополнительные команды

### Локально (development)
```bash
npm run dev        # Запуск в режиме разработки
npm run build      # Сборка TypeScript
npm start          # Запуск собранного проекта
```

### Инициализация базы данных
```bash
# Создание таблиц (автоматически при запуске)
curl http://localhost:3001/api/init-db

# Заполнение тестовыми данными
curl http://localhost:3001/api/seed-db

# Проверка соединения с БД
curl http://localhost:3001/api/db-check
```

## 🎯 Следующие шаги

1. ✅ Деплой на Render
2. ✅ Настройка webhook в Telegram
3. ✅ Тестирование всех функций
4. 🔄 Настройка платежной системы (YooKassa, Stripe)
5. 🔄 Добавление реальных каналов для подписки
6. 🔄 Настройка уведомлений
7. 🔄 Мониторинг и аналитика

## 📞 Поддержка

- Email: support@skinfactory.com
- Telegram: @SkinFactoryArBot
- GitHub: https://github.com/bhgou/tg_frontend

---

**Версия:** 2.0.0  
**Дата обновления:** 2026-02-13  
**Статус:** ✅ Production Ready
