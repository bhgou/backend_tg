# Интеграция Фронтенда с Бэкендом

## 📋 Обзор

Полная интеграция фронтенда (Vite + React) с бэкендом (Node.js + Express) настроена. Авторизация через Telegram WebApp API полностью функциональна.

## ✅ Что настроено

### 1. API Клиент
- ✅ Axios экземпляр с базовой конфигурацией
- ✅ Интерцепторы запросов (автоматическое добавление токена)
- ✅ Интерцепторы ответов (обработка ошибок)
- ✅ Автоматическая редирект при 401

### 2. Авторизация через Telegram
- ✅ Telegram WebApp API интеграция
- ✅ Автоматическая авторизация при открытии в Telegram
- ✅ Ручная авторизация для тестирования
- ✅ Сохранение токена в localStorage
- ✅ Верификация токена при загрузке

### 3. Управление состоянием
- ✅ Zustand store для пользователя
- ✅ Persist middleware для сохранения данных
- ✅ Балансы (обычный и премиум)
- ✅ Статистика пользователя

## 🔧 Конфигурация

### Переменные окружения (.env.production)

```bash
# API
VITE_API_URL=https://backend-tg-i7mg.onrender.com/api
VITE_API_TIMEOUT=30000

# Telegram
VITE_TELEGRAM_BOT_USERNAME=SkinFactoryArBot
VITE_TELEGRAM_WEB_APP_URL=https://t.me/SkinFactoryArBot/skin_factory

# Приложение
VITE_APP_NAME=Skin Factory
VITE_APP_VERSION=2.0.0
VITE_SUPPORT_EMAIL=support@skinfactory.com
VITE_MAINTENANCE_MODE=false

# Функции
VITE_ENABLE_TELEGRAM_AUTH=true
VITE_ENABLE_PAYMENTS=true
VITE_ENABLE_WITHDRAWALS=true
VITE_ENABLE_MINI_GAMES=true

# UI
VITE_THEME=dark
VITE_LANGUAGE=ru
VITE_ANIMATIONS=true
```

## 🚀 Поток авторизации

### При открытии в Telegram

```
1. Пользователь открывает приложение через бота
   ↓
2. Telegram WebApp API передает данные пользователя
   ↓
3. Фронтенд отправляет данные на бэкенд (/auth/login)
   ↓
4. Бэкенд создает/обновляет пользователя в БД
   ↓
5. Бэкенд возвращает JWT токен и данные пользователя
   ↓
6. Фронтенд сохраняет токен и данные пользователя
   ↓
7. Пользователь авторизован и может пользоваться приложением
```

### При повторном открытии

```
1. Пользователь открывает приложение
   ↓
2. Фронтенд проверяет localStorage на наличие токена
   ↓
3. Если токен есть, отправляет запрос на верификацию (/auth/verify)
   ↓
4. Бэкенд проверяет токен и возвращает данные пользователя
   ↓
5. Пользователь авторизован
```

## 📡 API Эндпоинты

### Auth API

```typescript
// Регистрация/логин
POST /auth/login
Body: {
  telegramId: string;
  username?: string;
  firstName?: string;
  lastName?: string;
  photoUrl?: string;
  referralCode?: string;
}
Response: {
  success: boolean;
  token: string;
  user: User;
}

// Верификация токена
POST /auth/verify
Body: { token: string }
Response: {
  success: boolean;
  user: User;
}

// Выход
POST /auth/logout
Response: { success: boolean }
```

### User API

```typescript
// Профиль пользователя
GET /user/profile
Response: { success: boolean; data: User }

// Статистика
GET /user/stats
Response: { success: boolean; stats: UserStats }

// Ежедневная награда
POST /user/daily
Response: { success: boolean; newBalance: number }

// Рефералы
GET /user/referrals
Response: { success: boolean; referrals: Referral[] }

// История транзакций
GET /user/transactions?page=1&limit=20
Response: { success: boolean; transactions: Transaction[]; pagination: Pagination }
```

## 🔐 Безопасность

### На фронтенде
- ✅ Токен хранится в localStorage
- ✅ Автоматическая очистка при logout
- ✅ Автоматическая редирект при 401
- ✅ Токен добавляется в заголовки всех запросов

### На бэкенде
- ✅ JWT токен с expiresIn: '30d'
- ✅ Проверка токена в middleware
- ✅ CORS для разрешенных доменов
- ✅ Helmet для HTTP заголовков
- ✅ Rate limiting

## 🧪 Тестирование

### Локально

```bash
# Запуск бэкенда
cd backend_tg
npm run dev

# Запуск фронтенда
cd tg_frontend
npm run dev
```

### В Telegram

1. Откройте бота: `@SkinFactoryArBot`
2. Нажмите кнопку "🎮 Открыть приложение"
3. Автоматическая авторизация произойдет

### В браузере (тестовый режим)

1. Откройте: `http://localhost:5173`
2. Перейдите на `/auth`
3. Введите любой Telegram ID для теста

## 📝 Примеры использования

### Получение профиля пользователя

```typescript
import { userAPI } from './services/api';

const fetchProfile = async () => {
  try {
    const response = await userAPI.getProfile();
    if (response.success) {
      console.log('Профиль:', response.data);
    }
  } catch (error) {
    console.error('Ошибка:', error);
  }
};
```

### Обновление баланса

```typescript
import { useUserStore } from './store/user.store';

const { addBalance } = useUserStore();

// Добавить 100 CR
addBalance(100);

// Вычесть 50 CR
useUserStore.getState().deductBalance(50);
```

### Проверка авторизации

```typescript
import { useUserStore } from './store/user.store';

const { isAuthenticated, user } = useUserStore();

if (isAuthenticated && user) {
  console.log('Пользователь авторизован:', user.username);
}
```

## 🐛 Troubleshooting

### CORS ошибки

Проверьте переменную `VITE_API_URL` в `.env.production`:

```bash
VITE_API_URL=https://backend-tg-i7mg.onrender.com/api
```

### Авторизация не работает

1. Проверьте, что приложение открыто через Telegram
2. Проверьте консоль браузера на ошибки
3. Проверьте, что бэкенд запущен и доступен
4. Проверьте токен бота в `.env` на бэкенде

### Токен недействителен

```typescript
// Очистите localStorage и перезагрузите страницу
localStorage.removeItem('token');
location.reload();
```

## 📊 Мониторинг

### Логи

Фронтенд логирует важные события:

```javascript
console.log('🚀 Инициализация приложения...');
console.log('✅ API подключен');
console.log('✅ Токен валидный');
console.log('✅ Авторизация через Telegram успешна');
```

### API Requests

Все запросы логируются в интерцепторах:

```javascript
console.error('API Error:', {
  url: error.config?.url,
  status: error.response?.status,
  message: error.message,
  data: errorData,
});
```

## 🎯 Следующие шаги

1. ✅ Настроить вебхук в Telegram BotFather
2. ✅ Задеплоить бэкенд на Render
3. ✅ Задеплоить фронтенд на Vercel
4. ✅ Протестировать авторизацию
5. 🔄 Добавить платежную систему
6. 🔄 Добавить уведомления
7. 🔄 Настроить аналитику

## 📞 Поддержка

- Telegram: @SkinFactoryArBot
- Email: support@skinfactory.com
- GitHub: https://github.com/bhgou/tg_frontend

---

**Версия:** 2.0.0  
**Дата обновления:** 2026-02-13  
**Статус:** ✅ Production Ready
